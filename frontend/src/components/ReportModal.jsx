// TODO: Implement ReportModal component
export default function ReportModal({ isOpen, onClose }) {
  if (!isOpen) return null;
  
  return (
    <div className="modal">
      {/* TODO: Add modal content */}
    </div>
  );
}
